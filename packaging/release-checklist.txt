
Change version number
  ./meson.build
  README.md
  MANUAL.md (man page title line; then regenerate rmw.1 -- see below)
  packaging/debian/changelog: add a new entry. 'dch -r' updates the
  timestamp, but it needs the devscripts package; the entry can also be
  written by hand in the same format.

## canfigger.wrap

Make sure the revision points to a release tag.

For later Debian package creation, check standards version
  https://www.debian.org/doc/debian-policy/upgrading-checklist.html


update po and pot files
  meson compile rmw-pot
  ninja rmw-update-po

Formatting checks, from the project root:

  meson fmt -eir
    (-e honours .editorconfig, -i rewrites in place, -r recurses into
    subprojects)

  Check the C sources against .editorconfig -- no tabs, no CRLF, and a
  newline at the end of every file. src/bsdutils/ is vendored; skip it:

grep -rlP "\t|\r" --include="*.c" --include="*.h" src test | grep -v bsdutils

    (no output = clean; for the final-newline rule, `tail -c1` on a file
    should print nothing)

  Do NOT run `indent` over the whole tree. GNU indent does not know the
  GLib macros or typedefs: it joins G_GNUC_BEGIN/END_IGNORE_DEPRECATIONS
  onto the following statement and rewrites `GList *x` as `GList * x`. The
  flags in CONTRIBUTING.md are for hand-checking a single new function.

Add release date to ChangeLog
Ex:
2021-04-25

  * rmw v0.7.07 released


Change Month, Year, and version in the title line of MANUAL.md, then
regenerate the man page:
  `ninja -C <builddir> regen-man`   (writes rmw.1; commit both)
Check copyright date/year
  MANUAL.md (COPYRIGHT section)
  packaging/debian/copyright

Update the website manual (single-sourced from MANUAL.md):
  in the rmw-website repo, run `./sync-manual.sh` to regenerate rmw_man.md
  from MANUAL.md, then commit it (Jekyll renders it to rmw_man.html)

commit changes (NOTE: meson dist only packages *committed* changes), push,
open PR

Trigger release builds by using the dispatch event (select the unmerged branch
as the target)

Make the AppImage
  Done in the CI by packaging/appimage/build-appimage.sh, which uses
  quick-sharun from https://github.com/pkgforge-dev/Anylinux-AppImages

Make Debian package
  Done in the CI using https://github.com/andy5995/gh-action-build-deb

Test package creation with the PKGBUILD.

Test the SlackBuild in the slackbuild-full docker container. The SlackBuild
is maintained in ~/src/slackbuilds/system/rmw/ (a clone of the
SlackBuilds.org repo), not in this repo; bind-mount that directory into
the container.

MERGE PR to master

Publish the release
  Download the artifacts from the release workflow
  paste in the Changelog and attach the artifacts

Close milestone (https://github.com/theimpossibleastronaut/rmw/milestones)

Submit the updated SlackBuild to Slackbuilds.org (https://slackbuilds.org/guidelines/)
Use sbo-maintainer-tools to check for problems.
https://slackbuilds.org/repository/15.0/system/sbo-maintainer-tools/

sbo-maintainer-tools (sbolint and sbopkglint for SBo maintainers)

These are "lint" tools to make life easier for SlackBuilds.org
maintainers and admins:

sbolint: checks your SlackBuild, README, .info file, and slack-desc.
sbopkglint: checks your package, after it's built.
sbofixinfo: attempts to fix common problems with SBo .info files.
pre-commit-sbolint: git hook you should use, that automatically calls
sbolint and stops you from committing bad code.
sbodl: downloads source URLs, optionally using the SBo source archive.

Update latest release info on /docker/README.md

Tag latest release with version number and push to docker hub

Post-release version bump: set the version in ./meson.build to the next
patch level (e.g. 0.10.1). There is no '-dev' suffix; the version between
releases is just the next version number.
